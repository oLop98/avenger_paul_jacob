{% extends 'base.html.twig' %}

{% block content %}
    <div class="container mt-4">
        <h2 class="mt-4 text-center">Ajouter un nouvel auteur</h2>
        <div class="row justify-content-center">
            <div>
                {{ form_start(formAuteur) }}
                {{ form_row(formAuteur.nom)}}
                {{ form_row(formAuteur.prenom) }}
                <button type="submit" class="btn btn-primary">Ajouter</button>
                {{ form_end(formAuteur) }}
            </div>
        </div>
        {% if auteurs is not empty %}
            <table class="table">
                <thead>
                    <tr>
                        <th>Nom</th>
                        <th>Prenom</th>
                        <th>En cours de dev</th>
                    </tr>
                </thead>
                <tbody>
                    {% for a in auteurs %}
                    <tr>
                        <td>{{ a.nom }}</td>
                        <td>{{ a.prenom }}</td>
                        <td><a href="{{ path('contenuauteurs', {'id': a.id}) }}" class="btn btn-primary">Voir les détails</a></td>
                    </tr>
                {% endfor %}
                
                </tbody>
            </table>
        {% else %}
            <p>Aucun Auteur trouvé.</p>
        {% endif %}


    </div>
{% endblock %}
